global:
  scrape_interval: 15s

scrape_configs:

  - job_name: "marketdata-adapter-handler"
    metrics_path: /metrics

    static_configs:
      - targets:
          - marketdata-adapter-handler:8080